import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
async function reader(){const s=await createClient();const {data:{user}}=await s.auth.getUser();return user?.app_metadata?.role==='reader'?s:null}
export async function PATCH(req:NextRequest){const s=await reader();if(!s)return NextResponse.json({error:'Forbidden.'},{status:403});const b=await req.json().catch(()=>null);if(b?.kind==='rule'){if(!b.id)return NextResponse.json({error:'Rule id required.'},{status:400});const {error}=await s.from('availability_rules').update({weekday:Number(b.weekday),start_time:b.start_time,end_time:b.end_time,timezone:String(b.timezone||'Asia/Kolkata'),is_active:!!b.is_active}).eq('id',b.id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}if(b?.kind==='exception'){if(!b.id)return NextResponse.json({error:'Exception id required.'},{status:400});const {error}=await s.from('availability_exceptions').update({exception_date:b.exception_date,is_available:!!b.is_available,start_time:b.start_time||null,end_time:b.end_time||null,note:String(b.note||'').slice(0,500),timezone:String(b.timezone||'Asia/Kolkata')}).eq('id',b.id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}return NextResponse.json({error:'Invalid availability request.'},{status:400})}
export async function POST(req:NextRequest){const s=await reader();if(!s)return NextResponse.json({error:'Forbidden.'},{status:403});const b=await req.json().catch(()=>null);if(b?.kind==='rule'){const {data,error}=await s.from('availability_rules').insert({weekday:Number(b.weekday),start_time:b.start_time,end_time:b.end_time,timezone:String(b.timezone||'Asia/Kolkata'),is_active:true}).select('id').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json(data)}if(b?.kind==='exception'){const {data,error}=await s.from('availability_exceptions').insert({exception_date:b.exception_date,is_available:!!b.is_available,start_time:b.start_time||null,end_time:b.end_time||null,note:String(b.note||'').slice(0,500),timezone:String(b.timezone||'Asia/Kolkata')}).select('id').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json(data)}return NextResponse.json({error:'Invalid availability request.'},{status:400})}
